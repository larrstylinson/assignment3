<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cityName %> Photos</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            justify-content: center;
            align-items: center;
        }

        .background-image {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        .navbar {
            background-color:#FFFFFF7F;
            width: 100%;
            height: 53px;
            margin: 20px auto;
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }

        .navbar a {
            text-decoration: none;
            color: black;
            font-weight: bold;
            font-size: 23px;
            margin-left: 20px;
            margin-right: 50px;
        }
        h1{
            font-family: Andale, monospace;
            font-size: 53px;
            margin-top: -15px;
        }

        form label {
            margin-top: -32px;
            font-family: Impact, monospace;
            font-size: 21px;
            display: flex;
            margin-bottom: 13px;
            margin-left: 18px;
            margin-right: 15px;
            width: 168px;
            text-align: right;
        }

        form input {
            width: 87%;
            padding: 8px;
            margin-bottom: 12px;
            margin-top: -20px;
            font-size: 16px;
            border-radius: 10px;
            border-color: white;
        }
        form button {
            width: 32%;
            padding: 7px;
            border-color: black;
            border-radius: 10px;
            background-color: #578591;
            color: black;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-top: -2px;
        }

        form button:hover {
            background-color: #7ba4b6;
        }



        .photo-container {
            text-align: center;
            background-color: #FFFFFFCC;
            padding: 20px;
            border-radius: 10px;
            box-shadow: #00000019;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 392px; /* Измените на нужную ширину */
            height: 364px;
        }
        footer {
            margin-top: auto;
            background-color: #FFFFFF66;
            text-align: center;
            font-style: italic;
            border-top: 1px solid #cccccc;
            width: 100%;
            height: 43px;
        }
    </style>
</head>
<body>
<img src="/pics/pic3.jpg" alt="Background Image" class="background-image">
<div class="navbar">
    <a href="/register">Register</a>
    <a href="/weather">Weather</a>
    <a href="/get-time">Time</a>
    <a href="/city-info">City-info</a>
    <a href="/history">History</a>
    <a href="/admin">admin</a>
</div>


<div class="photo-container">
<h1>Photos of <%= cityName %></h1>
<form action="/city" method="post">
    <label for="cityName">Enter city name:</label>
    <input type="text" id="cityName" name="cityName" required>
    <button type="submit">Submit</button>
</form>

    <% if (photos && photos.length) { %>
        <img class="photo" src="<%= photos[0] %>" alt="Photo of <%= cityName %>" style="max-width: 150px; max-height: 150px;">
    <% } else { %>
        <p class="no-photos">No photos found for <%= cityName %></p>
    <% } %>


</div>
<footer>
    <p>Omar Marzhan SE-2230</p>
</footer>
</body>
</html>
